{
  "name": "smart-ai-support-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  },
  "devDependencies": {}
}
